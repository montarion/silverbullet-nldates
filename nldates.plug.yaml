name: nldates
functions:
  parseDate:
    path: nldates.ts:parseDate
    command:
      name: "parse date"
  parseDatecmd:
    path: nldates.ts:parseDatecmd
    command:
      name: "parse date command"
